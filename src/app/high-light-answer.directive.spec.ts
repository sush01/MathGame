import { HighLightAnswerDirective } from './high-light-answer.directive';

describe('HighLightAnswerDirective', () => {
  it('should create an instance', () => {
    const directive = new HighLightAnswerDirective();
    expect(directive).toBeTruthy();
  });
});
